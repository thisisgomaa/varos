> **Status:** current — consolidated index of every plan/spec found on 2026-09-23; an index, not a decision (charter §3).
# Varos Plan Map — every roadmap, plan and spec in one place (2026-09-23)

**What this is:** one page that lists every planning document in the repo, what each one planned, what is really built (checked against code, tests and commits), what is still open, and which old plans are dead. **What this is not:** a decision. Where this page and an ADR, the charter or `STATUS.md` disagree, those win (charter §3). Historical docs were only read, never edited.

**Baseline:** `main` at `3877dd4` (2026-09-23), Mac working copy `~/Documents/AI workspace/varos`. Evidence method: `grep` over `varos/crates/**.rs`, `git log`, `wc -l`, and reading every doc listed in §2. Commit hashes cited below were checked with `git log`. **Warning for readers of old docs:** the history was rewritten on 2026-07-07 (`logo.ai` removal, `MASTER_PLAN_V1_LAUNCH.md` §8 row 3). Some hashes quoted in pre-07-07 docs no longer exist: `451ca2a` (wgpu bump) is now `a3d540f`, `0b587f2` (layers Stage A) is now `052610c`, `8f8ca45` (simple Layers panel) is now `bc62c70`.

---

## 1. ملخص بالمصري (لأحمد)

1. لقينا **59 وثيقة فيها خطط أو مواصفات** (15 قديمة في `history/`، 17 مرجع في `reference/` و`design-reference/`، والباقي حالية: الـADRs والميثاق وSTATUS ودفتر الأوجاع ودراسات النهارده). الجدول في §2 فيه كلهم.
2. **الترتيب الوحيد الساري دلوقتي:** نقل البرنامج للماك (شغال) ← **P11.2** (الزوم الفوري + تسريع الشاشة) ← **F4.2** وباقي دفتر الأوجاع ← قرار فرع P6 ← **F5 ثم F6** (تقسيم الملفات الكبيرة) ← **F7** (تنضيف المكتبات والأمان) ← **F8** (قفلة البرنامج التأسيسي). بعدها المنتج بالترتيب المقترح: **الماسكات 3-6 ← تصدير PNG ← تصدير SVG ← استيراد SVG**، وده انت اللي هتعتمده في F8.
3. **اللي اتبنى فعلًا (متأكدين من الكود):** القلم والأشكال والدمج (Pathfinder)، الطبقات البسيطة مقسومة بالأرت-بورد، الأرت-بوردات، السناب والمساطر والجايدز، الدوران الحي زي Illustrator، البيكر والعجلة، حفظ `.vrs` (وهو PDF)، الواجهة الجديدة بالصناديق، أول مرحلة تسريع (أسرع 10 مرات)، و239 تست.
4. **الخمس حاجات الجاية:** (١) نخلّص نقل البرنامج للماك، (٢) P11.2، (٣) F4.2 (قائمة File تشتغل بجد من باب واحد)، (٤) باقي الأوجاع + قرارك في P6 (والفرع ده موجود **بس** على الهارد الخارجي القديم — لازم يترفع GitHub قبل ما يضيع)، (٥) F5.
5. **قرارات مستنياك:** MCP (ADR-0008)، تجربة المتصفح (تعديل ADR-0001)، اختياراتك من دراسة Compositor، مكان الكنترول-بار (P8)، وترتيب F4.2 قبل ولا بعد الأوجاع.
6. **خطط ماتت (متشتغلش عليها):** Tauri ولوحات الويب، الزجاج المغبّش، رخصة AGPL، لوحة الطبقات المعقدة بتاعة Illustrator، فورك Penpot، ملف `.varos` المضغوط، التحديث الصامت. التفاصيل في §5.
7. **حاجات مش مكتوبة في أي خطة ومحتاجينها:** توقيع وتغليف نسخة الماك والويندوز، خطة الخطوط (وفكرتك إن المستخدم يرفع خطوطه على Drive مركونة لحد ما نسخة الديسكتوب تثبت)، حماية الشغل من القفل من غير حفظ، وخطة الـStroke الكاملة اللي طلبتها ومتكتبتش. التفاصيل في §6.

---

## 2. Every planning doc found

**Count:** 59 documents that plan, specify or decide something. Scanned in total: 68 first-party `.md`/`.html` files (63 tracked under `docs/`, plus `README.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `design-reference/design-system.md`, and one untracked audit draft), plus the `design-reference/` and `varos-spike/` folders. Records that plan nothing (GATE_LOG, INVENTORY, DEPENDENCY_MAP, F3_CHARACTERIZATION, ADR-0000 template, the two HTML pen prototypes, CONTRIBUTING, VENDOR_PATCHES) are listed at the end of this section.

"Date" = the date the doc gives itself, else its first commit. "Authoritative?" follows charter §3: ADRs > charter > STATUS > specs referenced by an open work order > everything else.

### 2.1 Current authority (levels 1-3)

| Path | Date | Stamp | What it plans (1 line) | Still authoritative? |
|---|---|---|---|---|
| `docs/adr/ADR-0001-native-gpu-ui-stack.md` | 2026-07-11 | current | Native Winit + Egui + WGPU only; no Tauri/web panels; CPU renderer = future option | **Current (level 1)** |
| `docs/adr/ADR-0002-two-level-command-boundary.md` | 2026-07-11 | current | `EditCommand` in core + `AppCommand` in app; closed enums are not a plugin API | **Current** — first half built (F4.1), second half = F4.2 |
| `docs/adr/ADR-0003-v1-container.md` | 2026-07-11 | current | `.vrs` = PDF with embedded versioned JSON | **Current** |
| `docs/adr/ADR-0004-v1-schema-policy.md` | 2026-07-11 | current | V1 schema = Serde JSON; no introspectable inspector/plugin/AI schema promised | **Current** |
| `docs/adr/ADR-0005-crate-dependency-directions.md` | 2026-07-11 | current | 4 crates + fork; allowed/forbidden edges, CI-enforced | **Current** |
| `docs/adr/ADR-0006-egui-tiles-fork-contract.md` | 2026-07-11 | current | Vendored `egui_tiles` patch contract (5 files) | **Current** |
| `docs/adr/ADR-0007-visible-update-policy.md` | 2026-07-11 | current | Updates visible and user-controlled; silent update rejected | **Current** |
| `docs/foundation/FOUNDATION_CHARTER.md` | 2026-07-11 | current | Work orders F1-F8, gates, parked-risk flags, proposed product sequence (§8) | **Current (level 2)** |
| `docs/foundation/STATUS.md` | 2026-09-23 | current | Active order (P11.2), queue, Mac move, autonomous-run process, flags | **Current (level 3)** |

### 2.2 Current specs / plans / logs (level 4-5, current stamp)

| Path | Date | Stamp | What it plans (1 line) | Still authoritative? |
|---|---|---|---|---|
| `CLAUDE.md` | 2026-07-06 | current | Standing session rules; hard architecture and visual laws | Current (rules); see §5 row "push" for one conflict with STATUS |
| `README.md` | 2026-07-06 | current | Public "Working today / Coming next / Vision" table | Current but partly stale (says new UI "being merged" — merged `b4031d5`) |
| `docs/VAROS_CONSTITUTION.md` | 2026-06-23 (amended 07-11) | current | Mission, GPL, Windows-first, v1 finish line (#14), way of working | Current, as amended by ADR-0001/0004/0007 |
| `docs/UI_DIRECTION.md` | 2026-07-03 | current | The visual law: 7 rules, ONE-HOME, box container model, standard layout | **Current** (visual law) |
| `docs/LAYERS_VISION.md` | 2026-07-03/06 | current | Simple Layers panel, masks model + gestures, bands (deferred), sections by artboard | Current design of record for Layers/masks |
| `docs/MASKS_PLAN.md` | 2026-07-09 | current | Masks in 6 gated stages | Current; stages 1-2 built, 3-6 open |
| `docs/PAINS_LOG.md` | 2026-07-08 → 07-12 | current | Ahmed's pain log: P1-P11, A1-A32, FB1-FB8, deferred talk items | Current (open items feed the queue after P11.2) |
| `docs/VISUAL_POLISH_PLAN.md` | 2026-07-09 | current | A16 icons, A17 picker, A14 bar — 11 ranked fixes | Current; the safe-mechanical wave shipped (`5684231`: filled align icons, bar sizing, picker layout, real pipette); ranks 8-10 need Ahmed's eye |
| `docs/foundation/F4_DESIGN.md` | 2026-07-12 | current | F4.1 `EditCommand` design; names F4.2 scope (file lifecycle, dialogs, panels) | Current evidence; F4.2 scope pointer (§"Additional call sites", line 143) |
| `docs/foundation/P11_1_PERF.md` | 2026-07-12 | current | P11.1 evidence; reserves the cross-frame flatten cache for P11.2 | Current evidence |
| `docs/foundation/OWNERSHIP_MAP.md` | 2026-07-11 | current | `ui.rs` / `editor.rs` responsibility maps = the F5/F6 split plan | Current input to F5/F6 (line counts now stale: `ui.rs` is 5,811, `wc -l`) |
| `docs/audits/2026-07-11-CODEX-FULL-PROJECT-AUDIT.md` | 2026-07-11 | current | Risk register; 6-phase remediation program (§15) | Current as risk register; its sequence superseded by charter §8 |
| `docs/audits/2026-07-11-CLAUDE-COUNTER-REVIEW.md` | 2026-07-11 | current | Accepts audit phases with amendments; 7 decisions for Ahmed | Current as risk register; sequence superseded by charter §8 |
| `docs/audits/2026-09-23-CARGO_AUDIT_TRIAGE.md` | 2026-09-23 | current (**untracked, not committed**) | Provisional dependency-advisory triage (9 advisories, manual) | Draft of F7's "advisory triage doc"; awaits Ahmed's approval |
| `docs/studies/2026-09-23-MCP_CONTROL_STUDY.md` | 2026-09-23 | reference | AI agents drive Varos via MCP over `EditCommand`; 6 stages; asks ADR-0008 | Proposal only |
| `docs/studies/2026-09-23-COMPOSITOR_UI_STUDY.md` | 2026-09-23 | reference | 13 UI behaviours to borrow; U1-U3 next pieces; pixel features fit list | Proposal only |
| `docs/studies/2026-09-23-ONLINE_AND_MAC_STUDY.md` | 2026-09-23 | reference | Mac native (A), browser WASM (B), Figma-style cloud (C); staged path | Proposal only |
| `docs/foundation/NOTES_FOR_CHARTER.md` | 2026-07-11 | historical | Extraction preconditions (incl. P6 fate before F5) | Absorbed into the charter |

### 2.3 Historical (`docs/history/`, 15 plan docs)

| Path | Date | Stamp | What it plans (1 line) | Still authoritative? |
|---|---|---|---|---|
| `docs/history/VAROS_START_HERE.md` | 2026-06-25 | historical | Pen-feel web spike brief; later "Rust core + Tauri web panels" | Historical; architecture superseded by ADR-0001 |
| `docs/history/VAROS_PLAN.md` | 2026-06-25 | historical | Phases 0-5: Rust port spike → paint → arrange → combine → text+export → v1 | Historical; superseded by DETAILED_ROADMAP, then charter |
| `docs/history/RUST_PORT_SPIKE_NOTES.md` | 2026-06-25 (decision 06-23) | historical | Port pen + rectangle to Rust + Tauri + wgpu | Historical; spike passed (`varos-spike/`), Tauri dropped |
| `docs/history/ROADMAP.md` | 2026-06-27 | historical | Phase 1 finish UI → 2 usable → 3 toolset → 4 text/export/save → v1; deferred list | Self-marked superseded (2026-06-29) by plan.html + DETAILED_ROADMAP |
| `docs/history/DESIGN_BRIEF.md` | 2026-06-27 | historical | Apply the BStudio dark+azure design to the wry web panels | Superseded by UI_FIGMA_SPEC → UI_DIRECTION |
| `docs/history/PANELS_PRO_SPEC.md` | 2026-06-26 | historical | Solid docked panels; right dock = two tab groups; pro number inputs | Superseded by UI_DIRECTION (its tab-group structure survived) |
| `docs/history/GPU_UI_SPIKE_BRIEF.md` | 2026-06-27 | historical | egui on our wgpu surface, never eframe; wgpu bump as Task 0 | Historical; spike done (`ff6ff34`, `a3d540f`) |
| `docs/history/UI_PLAN_EN.md` | 2026-06-27 | historical | English handoff of the native GPU UI plan | Historical detail only |
| `docs/history/UI_MASTER_PLAN.md` | 2026-06-27 | historical | Arabic UI plan: full-bleed board, floating panels, zero black | Superseded by UI_DIRECTION (docked homes) |
| `docs/history/DETAILED_ROADMAP.md` | 2026-06-29 → 07-06 | historical | Exhaustive 12-system roadmap, Stage 1/2/3, locked decisions D1-D10 | **Historical detail only** — best per-feature backlog; status markers stale |
| `docs/history/plan.html` | 2026-06-29 → 07-06 | historical | Dashboard of the same roadmap (ORDER / DECISIONS / FOUNDATION / SYSTEMS arrays) | Historical; superseded by STATUS |
| `docs/history/MASTER_PLAN_V1_LAUNCH.md` | 2026-07-06 (upd. 07-08) | historical | Close-out A1-A5 → packaging B → beta C → text T → launch L0-L3; §5.5 Ahmed's requests | Historical; §5.5 requests still owed (see §4); sequence superseded by charter §8 |
| `docs/history/PAINS_WORK_ORDER.md` | 2026-07-08 | historical | Pain waves 1-5 (execution order + acceptance) | Done/closed; results in PAINS_LOG |
| `docs/history/PAINS_WORK_ORDER_2.md` | 2026-07-08 | historical | T1-T11 autonomous sweep; A30 and P6 specs | Done except T11 (P6) on a branch |
| `docs/history/A7_LIVE_TRANSFORM_PLAN.md` | 2026-07-09 | historical | Live per-object rotation in 7 stages | Done (`2f5db36`, `138b512`) |

### 2.4 Reference (`docs/reference/`, 16 docs) + outside `docs/`

| Path | Date | Stamp | What it plans (1 line) | Still authoritative? |
|---|---|---|---|---|
| `docs/reference/VECTOR_BUILD_SPEC.md` | 2026-06-25 | reference | Part A Illustrator anchor model; **Part B Penpot fork**; Part C Arabic summary | Part A = reference; Part B dead |
| `docs/reference/ILLUSTRATOR_TOOLS_CATALOG.md` | 2026-06-24 | reference | CORE/LATER/SKIP tool list + 15-step build order | Reference (tool priority) |
| `docs/reference/SHAPES_NOTES.md` | 2026-06-25 | reference | Shapes as plain paths; kappa circle; star | Reference; mostly built |
| `docs/reference/FILL_STROKE_NOTES.md` | 2026-06-25 | reference | Per-object fill/stroke model (web-era) | Reference; built in Rust form |
| `docs/reference/UI_FIGMA_SPEC.md` | 2026-06-27 | reference | Exact palette/fonts/cursors from Ahmed's Figma | Palette/fonts/cursors valid; layout superseded by UI_DIRECTION |
| `docs/reference/ELEMENTS_CATALOG.md` | 2026-06-29 | reference | Every tool/panel/system of a pro vector tool + "possibly missing" list | Reference (completeness checklist) |
| `docs/reference/ARTBOARD_SPEC.md` | 2026-06-30 | reference | Artboard Stage 1/2/3 + locked decisions | Reference; Stage 1-2 largely built |
| `docs/reference/SNAP_TRANSFORM_SPEC.md` | 2026-06-30 | reference | Snap engine + transform completeness, Stage 1/2/3, 14 decisions | Reference; Stage 1 + parts of 2 built |
| `docs/reference/SNAP_TRANSFORM_FEATURES.md` | 2026-06-30 | reference | Web-sourced feature matrix (v1 / Stage 2 / Stage 3 / skip) | Reference |
| `docs/reference/SNAP_TRANSFORM_REFERENCE.md` | 2026-06-30 | reference | Verified Illustrator/Affinity/Figma shortcut & snap facts | Reference |
| `docs/reference/SAVE_EXPORT_PLAN.md` | 2026-06-30 | reference | PDF-native save (CBOR, krilla), CMYK, Arabic-in-PDF, export sinks (tiny-skia, SVG) | Reference; container shape superseded by ADR-0003 (JSON, pdf-writer) |
| `docs/reference/COLOR_SPEC.md` | 2026-07-02 | reference | Picker Stage 1/1b → Swatches (2) → Gradients (3) → later | Reference; Stage 1/1b built, 2-3 open |
| `docs/reference/LAYERS_SPEC.md` | 2026-07-03 | reference | Illustrator-grade Layers panel spec + staged rebuild | Superseded on the panel surface by D10 / LAYERS_VISION |
| `docs/reference/BOX_SYSTEM_PLAN.md` | 2026-07-03 | reference | Shell build: tokens → void → box sandbox → behaviours → migrate; Stage 5 later | Reference; Stages 0-4 built (`b4031d5`), Stage 5 open |
| `docs/reference/UI_VISION_MOCKUP.html` | 2026-07-03 | reference | The living visual reference named by UI_DIRECTION | Reference (visual law's picture) |
| `docs/reference/ENGINEERING_REVIEW.md` | 2026-07-05 | reference | 10-item engineering backlog (CI, unsafe pass, golden corpus, splits…) | Reference; most items absorbed by the charter |
| `design-reference/design-system.md` | 2026-06-27 | reference | BStudio (pre-Varos) token system; plus `mockups/` + `editor-redesign/` React prototype | Reference only (palette ancestry) |

### 2.5 Non-plan records (scanned, not counted)

`docs/foundation/GATE_LOG.md` (gate trail) · `docs/foundation/INVENTORY.md` (F1 frozen register) · `docs/foundation/DEPENDENCY_MAP.md` · `docs/foundation/F3_CHARACTERIZATION.md` · `docs/adr/ADR-0000-template.md` · `docs/VENDOR_PATCHES.md` (contract + rebase procedure) · `CONTRIBUTING.md` · `docs/history/prototypes/pen-spike.html` + `varos-pen-spike.html` (the approved pen feel, kept as reference per Constitution #18). **`varos-spike/`** = the June 2026 Rust port spike (winit 0.29 + wgpu 0.19, pen + rectangle); outside the workspace; its fate is an F7 decision for Ahmed (charter §5).

---

## 3. System-by-system map

Legend: **Planned** = where the plan lives · **Built** = verified in code/tests/commits (not trusted from ✅ markers) · **Pending** = open items with their stage · **Decide** = open questions for Ahmed.

### 3.1 Foundations (architecture, model, commands, gates)
- **Planned:** DETAILED_ROADMAP Layers A/B/C + decisions D1/D3/D5/D6; charter F1-F8; ADR-0002/0004/0005/0006; MASTER_PLAN §10 binding commitments (golden corpus, Arc-wrap image bytes, "plugin foundation"); ENGINEERING_REVIEW §5; audit §15 Phases 4-5.
- **Built:** serde spine `04a3525` + units `ed32b28` (`varos-core/src/units.rs:21`); golden `.vrs` corpus `976a334` + round-trip law `1753e9d` (merged F3 `b8c9ba6`); `EditCommand` in core (`varos-core/src/command.rs:12`, F4.1 `bd8bc1f`); dependency-direction gate `3a0ad3b` (`tools/check_dep_directions.ps1`); vendor contract `76640fe`; docs stamped and laid out (F2a/F2b). **239 tests**: `grep -rn '#\[test\]' varos/crates` = 239 (core 194 · app 19 · pdf 13 · render 13), matching STATUS.
- **Pending:** F4.2 `AppCommand` (0 hits for `enum AppCommand`); F5 split `ui.rs` (now **5,811** lines by `wc -l`; STATUS dashboard still says 5,563); F6 split `editor.rs` (4,533); F7 toolchain/MSRV, `.gitattributes`, advisory triage (draft exists, untracked), hostile-file hardening, `varos-spike` fate; F8 review + close. Introspectable schema / property registry (DETAILED B6) = future only (ADR-0004). `ToolKind` is still a closed enum (`varos-core/src/editor.rs:33-46`), so the "plugin foundation" (MASTER_PLAN §10-3) has not started. Undo still stores full document clones (P11.3, "measure first"). winit `ApplicationHandler` migration (audit P1.6).
- **Decide:** order of F4.2 vs remaining pains (see §6 gap G11); `varos-spike` keep/delete (F7).

### 3.2 Artboard / Document
- **Planned:** DETAILED_ROADMAP "Artboard / Document"; ARTBOARD_SPEC Stages 1-3; LAYERS_VISION §9 (D11); pains A8a, A11, A27, A28, A30.
- **Built:** model + tool + clip `d1e5f80`, rail/inspector/on-canvas chrome `7685ad8` (`struct Artboard` at `varos-core/src/model.rs:337`, `ToolKind::Artboard`); multi-board, sections-by-artboard `c40d5fc`, cross-board drag + page eye/lock `96b09ec`, Alt-dup + board snapping `d96ca25`; boardless start A8a `c65e64a`; Shift multi-select + bar clip/colour/move-art A27/A28 `d29327c`; per-object release-from-clip A30 `f2dd7bd` (`clip_exempt`, `model.rs:310`). Tests: `varos-core/tests/boards.rs`.
- **Pending:** New Document dialog + presets library (ARTBOARD_SPEC Stage 1-2 — `File ▸ New` row is visual-only, `varos-app/src/ui.rs:3294`); dedicated Artboards panel (none in `PanelId`, `varos-app/src/shell/registry.rs:10-19`); Rearrange All, artboard options dialog (Stage 2); print marks/bleed UI (Stage 3); right-click "Release clip" menu (A30 note: object context menu does not exist yet).
- **Decide:** artboard name chip redesign + double-click rename on canvas (parked 2026-07-11, PAINS_LOG "بنود نقاش مؤجّلة").

### 3.3 Transform + Snap
- **Planned:** DETAILED_ROADMAP Transform + Snapping (D7: build together); SNAP_TRANSFORM_SPEC Stages 1-3 (+ FEATURES, REFERENCE); A7_LIVE_TRANSFORM_PLAN; pains A9-A12, A20, A29; Compositor study §3.4 (whole-edge grab).
- **Built:** SnapEngine `8383e33` + magnet menu/HUD/Ctrl+U `89b06f6`; rulers `bb3f7ac`; guides `c186bfa` (`struct Guide`, `model.rs:493`); Rotate/Scale tools + Transform Again `ccc3678` (`EditCommand::TransformAgain`, `command.rs:77`); numeric X/Y/W/H/rotate with 9-point reference (`refpt`, `ui.rs:802`; `Op::SetBBox`, `ui.rs:95`); flip (`editor.rs:1364`); align/distribute (`editor.rs:1274`, A4 target mode `770cdda`); **live transform A7** `2f5db36` + `138b512` (`struct Xform`, `model.rs:29`; `Node.xform`, `model.rs:318`; `tests/live_transform.rs`, `tests/xform.rs`); input-feel T4 `25ad8b5`; A29 artboard-edge snap `9d76758`.
- **Pending (not found in code — 0 grep hits):** Shear tool, Reflect tool, Transform Each, Move… dialog, Offset Path, Free Transform (DETAILED Transform §4-§11 = Stage 1-2; SNAP_TRANSFORM_SPEC Stage 3). Pixel grid / snap-to-pixel (Stage 2-3, not verified). Whole-edge grab (Compositor 3.4).
- **Decide:** none open in current docs; SNAP_TRANSFORM_SPEC's decisions were locked 2026-06-30.

### 3.4 Colour
- **Planned:** DETAILED "Color system"; COLOR_SPEC Stage 1 → 1b modal → 2 Swatches → 3 Gradients → later (global/spot, recolor); MASTER_PLAN A4/A5; pains A5, A6, A17, A19; VISUAL_POLISH A17.
- **Built:** hand-painted picker `c1d809d`, floating modal `005c009`, Wheel + harmony `c7a96f4`; `Paint` enum migration `d556acf` (`model.rs:119-123` — variants `None` and `Solid` only); live preview + fill/stroke indicator + picker eyedropper + system-wide pick `607bbd7` (`GetPixel`, `varos-app/src/cursors.rs:375`); safe picker layout polish `5684231`. Tests: `tests/colors.rs`.
- **Pending:** Swatches panel (COLOR_SPEC Stage 2 — a dummy, deliberately not dockable: `registry.rs:22-25`); Gradients + Gradient tool G (Stage 3 — no `Gradient` variant exists); global/spot colours, Recolor Artwork, harmony-linked edits (later); needs-eye picker items (VISUAL_POLISH ranks 8-10).
- **Decide:** A17 full picker redesign; the parked "Codex colours" talk (PAINS_LOG). Note a tension: COLOR_SPEC says skip CMYK modes, SAVE_EXPORT_PLAN §4 describes a CMYK print model — no current doc resolves it.

### 3.5 Layers / Masks
- **Planned:** DETAILED "Layers / Structure" (D2, D10, D11); LAYERS_SPEC (panel surface, superseded); LAYERS_VISION (design of record); MASKS_PLAN (6 stages).
- **Built:** real scene graph `052610c`; simple panel `bc62c70`; sections by artboard `c40d5fc`; `paint_list()` seam `2a46009`; **masks stages 1-2** `9f0c830` (`enum GroupRole`, `model.rs:272`; `mask_child`, `model.rs:329`; `Document::clip_group`, `model.rs:1217`; `Group::Clip` in `varos-core/src/scene.rs:54`; `tests/masks.rs`); groups/z-order; occlusion hit-test A31; undo keeps selection P10. Tests: `tests/layers.rs`, `tests/groups.rs`, `tests/occlusion.rs`.
- **Pending:** masks **Stage 3** gestures (Ctrl+Alt+G + drag onto thumbnail — 0 hits for a make-clip action in `varos-app`), **Stage 4** hit-test/bbox, **Stage 5** PDF clip, **Stage 6** nested/rotation; soft masks alpha/luma (LAYERS_VISION §7.1, future); structural bands (§4, deferred); template layers (§7.2); Make/Release Compound Path, Isolation mode, Select Same, layer search (DETAILED Layers §11/13/15/18 — not found); eye swipe (Compositor 3.5).
- **Decide:** masks go first in the product sequence only when Ahmed ratifies charter §8 at F8.

### 3.6 Stroke
- **Planned:** DETAILED "Stroke system" (16 areas); FILL_STROKE_NOTES; plan.html note (07-03: corner nubs on thick strokes → caps/joins/align); pain A19; **MASTER_PLAN §5.5 F5** (write the full stroke spec now, build in stages).
- **Built:** weight only (`stroke_width` + `Op::SetStrokeW`); rendering improvements: corner-join elision `e644107` (`varos-render-wgpu/src/tess.rs:80-116`), stroke clipped at the artboard edge A2 `55448a0`. PDF writes round caps/joins everywhere (`varos-pdf/src/lib.rs:103`, `:156`).
- **Pending:** caps, joins + miter limit, alignment inside/outside, dashes, arrowheads, width profiles/Width tool, Outline Stroke, Scale Strokes & Effects (all Stage 1-3 in DETAILED; 0 grep hits for dash/cap/join models in core).
- **Decide:** nothing — but **the complete stroke spec Ahmed asked for on 07-08 was never written** (gap G5).

### 3.7 Save / File
- **Planned:** DETAILED "Save / File" (D1: `.varos` zip — dead) + PDF-interop backlog (a)-(d); SAVE_EXPORT_PLAN Save Stages 0-3; ADR-0003; charter §8 parked trust items; charter §9.2 `VRS_FORMAT.md`; MASTER_PLAN §5.3 autosave; audit P0.1/P0.2.
- **Built:** save/open `.vrs` `7a5b3c8`; `.vrs` = valid PDF + embedded model `b06194a` (`varos-pdf/src/lib.rs:30` `save_vrs`, `:36` `load_vrs`, `:72` `write_pdf`); versioned blob + legacy JSON load (`varos-core/src/file.rs`); atomic temp+rename write (`file.rs:50-54`); unsaved `*` in title (`varos-app/src/main.rs:148-150`); discard prompt **only when opening** another file (`main.rs:391`, `:414`, `:901`); golden corpus; single-instance P5 `18eb29e`.
- **Pending (all verified absent):** close-without-save guard — `CloseRequested` saves window state and exits (`main.rs:686-689`); autosave + crash recovery; `fsync`/`sync_all` in `write_atomic` (0 hits); recent files; working New/Open/Save/Export menu rows (visual-only, `ui.rs:3294-3299`); real multi-document (tabs are a `Vec<String>`, `ui.rs:812`); `VRS_FORMAT.md` (not written); hostile-file hardening (F7); PDF-interop backlog (whole-object `B` op, sRGB ICC tag, study Illustrator PDFs, foreign-PDF message).
- **Decide:** fake tabs removed vs real multi-doc (charter §9.4 says remove when `flag.release-milestone` fires); when to flip `flag.design-work-started` (it un-parks the trust items and jumps them ahead of everything).

### 3.8 Geometric tools
- **Planned:** DETAILED "Geometric-logic tools"; ILLUSTRATOR_TOOLS_CATALOG build order (#9 Shape Builder, #11 Line, #13 cut trio); VAROS_PLAN Phase 4; Constitution #14 ("combine = Pathfinder/Shape Builder" is on the v1 line); MASTER_PLAN §5.5 F1 (Live Corners) + F4 (Curvature).
- **Built:** `ToolKind` = Object, Direct, Pen, Rect, Ellipse, Triangle, Polygon, Convert, Eyedropper, Artboard, Rotate, Scale (`editor.rs:33-46`); pen add/delete/close/connect hints (`PenHint`, `editor.rs:51`); booleans Unite / MinusFront / Intersect / Exclude (`varos-core/src/boolean.rs:20-25`); sharp corners kept after booleans A26; delete-anchor opens the path A32 (`tests/delete_anchor.rs`, `tests/boolean_corners.rs`).
- **Pending:** Line Segment, **Shape Builder**, Scissors, Knife, Eraser, Pathfinder Divide, Make/Release Compound Path, Star, Curvature tool, Live Corners, Pencil/Smooth (all absent from `ToolKind`).
- **Decide:** Shape Builder is on the Constitution's v1 line but appears in no current sequence (charter §8 lists masks/export/import only).

### 3.9 Text / Arabic
- **Planned:** DETAILED "Text / Type" (Latin first, Arabic = Stage 3 moat); ELEMENTS §5; MASTER_PLAN §5.1 (rustybuzz + bidi + kashida; standalone MIT/Apache Arabic crate), stage T, launch moment L2; SAVE_EXPORT_PLAN §6 (Arabic in PDF: ToUnicode + ActualText); GPU_UI_SPIKE (Type tool on our own shaping canvas, not egui); Constitution #14 ("simple text" in v1) and #4 ("Arabic deferred to post-v1"); audit §15 Phase 6 item 6.
- **Built:** nothing. No text object, tool, shaping or font code in `varos/crates` (0 hits). `rustybuzz` is only a transitive dependency through `usvg` (ONLINE study §4.4).
- **Pending:** all of it — text model, Latin editing, Character/Paragraph homes, then Arabic shaping/BiDi/fallback/PDF export, then kashida.
- **Decide:** when Arabic starts. Current docs disagree in tone: Constitution #4 says "post-v1", CLAUDE.md/README say "Arabic-first moat", kashida was deferred by Ahmed on 2026-07-07 (MASTER_PLAN header). Font strategy (gap G3).

### 3.10 Export
- **Planned:** DETAILED "Export"; SAVE_EXPORT_PLAN §8 (tiny-skia raster, direct SVG serializer, PDF, PDF/X later); Constitution #14 (SVG/PNG in v1); charter §8 (PNG → SVG export → SVG import); Compositor study §3.9 (export sheet) and §3.10 (import report); MCP study (PNG for "eyes").
- **Built:** PDF only — the `.vrs` itself is a PDF; hidden boards/groups excluded `e0d07ae`; rotated objects exported (`varos-pdf/tests/rotated_export.rs`). The **Export…** menu row and the top-bar **Export** button are visual only (`ui.rs:3299`, `ui.rs:3227`).
- **Pending:** PNG export, SVG export, SVG import, export dialog, per-artboard filenames, clipboard copy, Export for Screens (Stage 2), PDF/X (Stage 3).
- **Decide:** ratify the export/import order at F8.

### 3.11 Effects / Appearance
- **Planned:** DETAILED "Effects / Appearance"; **MASTER_PLAN §5.5 F3** (Appearance stack; a short study doc is the mandatory first step); ELEMENTS §16; LAYERS_VISION §7.5; Compositor study §5.2 (blend modes, artwork shadows/blur post-v1).
- **Built:** per-object opacity and isolated group opacity (`tests/opacity.rs`). Nothing else.
- **Pending:** the Appearance study doc (not written); multiple fills/strokes; blend modes; live effects; Graphic Styles; Expand Appearance.
- **Decide:** F3 knowingly reverses the D10 simplification — confirm when its turn comes.

### 3.12 UI shell / panels
- **Planned (history):** GPU_UI_SPIKE_BRIEF, UI_PLAN_EN, UI_MASTER_PLAN, PANELS_PRO_SPEC, UI_FIGMA_SPEC, DESIGN_BRIEF. **Planned (current):** UI_DIRECTION + BOX_SYSTEM_PLAN + UI_VISION_MOCKUP; VISUAL_POLISH_PLAN; Compositor study U1-U3; MASTER_PLAN §5.5 F2 (tool-adaptive control bar); pains P6, P8, A3, A14, A16, A17.
- **Built:** native egui on wgpu `ff6ff34`; wgpu 29 / egui 0.35 / winit 0.30 `a3d540f`; box-system shell Wave 2 `b4031d5` (`shell/boxtree.rs`, `shell/tokens.rs`); frosted pipeline deleted `5ef070e`; safe polish wave `5684231`; dockable panels = Align, Pathfinder, Properties, Layers (`registry.rs:25`).
- **Pending:** BOX_SYSTEM Stage 5 (move/split/join, tear-off, workspaces, themes); Swatches/History/Assets bodies; P6 box-header redesign (**branch `codex/p6-header` exists only on the old external disk mirror** — not on GitHub, not in the Mac clone); needs-eye polish (VISUAL_POLISH 8-10); U1 number-field manners, U2 anchored bar, U3 status-bar hints; tool-adaptive bar (F2 request); DPI-change handling (parked, `flag.dogfooding`); hide Search/Share + remove fake tabs (`flag.release-milestone`; Search/Share/Export are visual mirrors, `ui.rs:3224-3229`).
- **Decide:** P8 (bar centred vs anchored left); P6 merge/drop (must be decided before F5); Compositor picks; Mac UI font (needs design sign-off, ONLINE study §3 item 2).

### 3.13 Performance
- **Planned:** PAINS_LOG P11 (diagnosis + escalation); P11.1 → P11.2 → P11.3 (STATUS); MASTER_PLAN §1 weakness #4 (benchmarks); audit §15 Phase 5 (budgets).
- **Built:** P11.1 `1931c80` — 10.4× on the 300%-zoom scene, 9.6× on curves-100, owner-verified (GATE_LOG §P11.1; `P11_1_PERF.md`; harness `varos-render-wgpu/examples/perf_harness.rs`).
- **Pending:** **P11.2** (active): (0) instant zoom — the A13 glide is still in `main.rs:604-609`; (1) path-level view culling + ring-level clipping for the 4000% zoom overdraw; (2) cross-frame flatten cache. P11.3 hover/snap costs (only if still felt); undo storage (measure first).
- **Decide:** nothing new (P11.2 already authorized 2026-07-12).

### 3.14 MCP / agents / plugins
- **Planned:** MASTER_PLAN §5.2 (schema → AI-native + WASM plugins) and §10-3 (plugin foundation); DETAILED B6/B1.6; ELEMENTS "possibly missing" #1; ADR-0002 (closed enums are not the plugin API); ADR-0004 (no schema promise); **MCP_CONTROL_STUDY** (15 tools, stages 0-6, new `varos-agent` crate).
- **Built:** only the enabling door — `EditCommand` (`command.rs:12`). No MCP, plugin or scripting code (0 hits).
- **Pending:** ADR-0008 decision, then study stages 1-6 (stage 5 "live mode" must wait for F4.2).
- **Decide:** ADR-0008 yes/no and timing. A new crate collides with charter §6 "no new crates" and ADR-0005's edge list, so the ADR must amend them explicitly.

### 3.15 Online / Mac
- **Planned:** Constitution #5 (Windows first, Mac/Linux later); ROADMAP/VAROS_PLAN (Mac/Linux post-v1); MASTER_PLAN §5.4 (no online collab before the editor is complete); DETAILED "Comments / Collaboration" (post-v1, local-first); **ONLINE_AND_MAC_STUDY** (A native Mac, B browser, C cloud); STATUS 2026-09-23 (Mac = primary machine).
- **Built:** `varos-core`, `varos-pdf`, `varos-render-wgpu` pass 220/220 on macOS (STATUS 2026-09-23). The `varos-app` Mac port is **not on `main`** (0 hits for `target_os = "macos"`; STATUS says its branch is pending).
- **Pending:** merge the port; then the study's 15-item Mac list (Retina changes, bundled UI font, settings path, window chrome, menu bar, pinch zoom, Finder open, cursors, eyedropper, `.icns`, bundle/DMG, **signing + notarization**, ~3-6 weeks); web-ready hygiene (Stage 0); browser spike (Stage 1, needs Ahmed); cloud after v1 (Stage 3).
- **Decide:** the study's three checkboxes (§8): approve Stage 0; approve/decline the spike; confirm cloud is parked until after v1.

### 3.16 Release / beta / launch
- **Planned:** MASTER_PLAN §2 B1-B5 (icon/empty state, installer + `.vrs` association, visible version + changelog, crash path, signing decision), §3 beta cohorts 0-3 + tasks + SmartScreen plan, §4 publish checklist, §6 L0-L3, §8 decisions; ENGINEERING_REVIEW #1 (CI); audit §15 Phase 3; charter §9.5; ADR-0007; STATUS trigger flags.
- **Built:** GPL-3.0 `LICENSE`, `TRADEMARK.md`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md` (DCO + relicensing clause); public repo `github.com/thisisgomaa/varos` (`git remote -v`); `.github/workflows/ci.yml` exists but hosted runners are blocked by the billing hold (STATUS "External action items"); icon files (`varos/varos.ico`, `icon.png`); crash dialog + panic hook (MASTER_PLAN §1).
- **Pending:** installer (B2), visible version/About + changelog (B3 — `CARGO_PKG_VERSION` unused, 0 hits), tester crash wording (B4), signing vs Microsoft Store (B5), no git tags / releases yet, no issue templates or SECURITY file (`.github/` holds only `workflows/`), branch protection (blocked), GitHub org + second admin (parallel track, no deadline), update mechanism (ADR-0007 = policy only).
- **Decide:** MASTER_PLAN §8 open rows — #2 signing vs Store, #5 beta cohort 1 names (critics said logo designers/illustrators), #6 devlog, #7 Arabic crate timing; flipping the four trigger flags (all `false` since 2026-07-11).

### 3.17 Community
- **Planned:** MASTER_PLAN §6.3 (good-first-issues, changelog credits, owner areas, BDFL, monthly release, Telegram-Arabic first), §6.4 (Ahmed's branding course as channel), §5.3 (template library, public benchmark, RTL UI option, visual history); README "areas that need owners".
- **Built:** CONTRIBUTING + CoC + honest README. Nothing else (no issues/labels/templates tracked in the repo).
- **Pending:** everything in §6.3; the Anthropic OSS grant follow-up (MASTER_PLAN §8 "applied, initial reply").
- **Decide:** community channel and devlog timing (tied to the release flags).

---

## 4. Ahmed's approved-but-not-built requests (so none is lost)

**Naming warning:** MASTER_PLAN §5.5 calls these **F1-F5**, which collides with the charter's work orders F1-F8. This page renames them **FR-1…FR-5**.

From `MASTER_PLAN_V1_LAUNCH.md` §5.5 (approved 2026-07-08; items 21-25 and 27 moved there from PAINS_LOG "الدفعة التانية"; FR-1 goes first by Ahmed's words):
1. **FR-1 Live Corners** (item 21) — corner widget inside the selected corner, drag = live radius with `R:` tooltip, Shift = rounded steps, "Corners" field in the bar; radius is a **non-destructive property**; later round/chamfer/inverted; check Figma corner smoothing first. Not built (no corner-radius model in core).
2. **FR-2 Tool-adaptive control bar** (item 22) — each tool registers its bar (pen: convert, handles, anchors, corners). Not built. Must be designed so future add-ons can register a bar.
3. **FR-3 Native Appearance system** (items 23 + 25) — multiple fills/strokes as ordered rows, per-row effects/modes. **First step: a short study doc** (not written). Not built.
4. **FR-4 Curvature tool** (item 24) — Illustrator's curvature drawing with live preview and double-click corner/smooth. Not built.
5. **FR-5 Complete stroke system** (item 27, "the last item in the batch") — weight, caps, joins + miter limit, align, dashes, dash caps, profiles, arrowheads; picker expands in Stroke mode (A19). **The full spec was to be written "now"** — it is not in the repo. Not built beyond weight.

From `PAINS_LOG.md` and related current docs (approved or authorized, not yet delivered):
6. **P11.2 instant zoom** — owner-authorized feel change 2026-07-12 (GATE_LOG §P11.1); glide still present (`main.rs:604-609`).
7. **Masks gestures** — BOTH drag-into-shape and Ctrl+Alt+G, named "Mask" (LAYERS_VISION §8.1, 2026-07-03); MASKS_PLAN Stage 3, not built.
8. **P6 box-header redesign** — spec in PAINS_WORK_ORDER_2 (remove ×, right-click Close/Close Tab Group, ☰ = panel options, pickers on grip/seam); built on `codex/p6-header`, **awaiting Ahmed's hand test; branch only on the old disk**.
9. **A30 right-click "Release clip"** on an object — the Properties toggle exists (`clip_exempt`), the object context menu does not.
10. **A19 full stroke options in the picker** — folded into FR-5.
11. **A17 full picker redesign, A16 full icon pass, A14 bar design** — needs Ahmed's eye (VISUAL_POLISH ranks 8-10).
12. **P8 control bar** — open choice: centred vs anchored left (Compositor U2 recommends anchored).
13. **P5 open-file-as-tab** — deferred until real multi-document; P5 itself needs a re-test (Ahmed said 07-11 "مش شغالة خالص").
14. **P4 double-click rename** on a Layers row — needs a re-test in the right place.
15. **A3 panels taller than their content** — waits on the P6 decision.
16. **Artboard name on canvas** — double-click rename + redesign of the size chip (parked 2026-07-11).
17. **"Codex colours"** — a talk Ahmed asked for (2026-07-09); no action yet.
18. **Charter §9.4 cleanups** (decided 2026-07-11, gated by `flag.release-milestone`): remove fake tabs, wire the File menu to existing actions, hide Search/Share until real.

---

## 5. Dead / contradicted plans

| Old plan (where) | Killed by (decision) |
|---|---|
| Rust core + **Tauri shell + web panels** (VAROS_START_HERE §8, VAROS_PLAN Phase 1, RUST_PORT_SPIKE_NOTES, DESIGN_BRIEF's wry panels, original Constitution 8-10) | Web panels removed 2026-06-27 (`ff6ff34`); **ADR-0001** (2026-07-11) |
| Promised **CPU fallback** renderer (VAROS_PLAN Phase 1, old Constitution) | ADR-0001: a future option, not a promise |
| **Silent background auto-update** (old Constitution #7) | **ADR-0007** |
| **Frosted glass** material + toggle (GPU_UI_SPIKE_BRIEF, UI_PLAN_EN, UI_MASTER_PLAN, ROADMAP Phase 1) | Ahmed 2026-07-02 (DETAILED_ROADMAP D9); code deleted `5ef070e` |
| **AGPL-3.0** licence (MASTER_PLAN §4.1, §6.2, §7 "L1") | Ahmed 2026-07-06: **GPL-3.0** (MASTER_PLAN §9.1; `LICENSE`) |
| **Illustrator-complex Layers panel** — target ring, select squares, colour bars, +layer/+sublayer, counters (DETAILED Layers §5-6, LAYERS_SPEC) | **D10** simple panel, Ahmed 2026-07-03 (LAYERS_VISION §0-1) |
| **Penpot fork** (VECTOR_BUILD_SPEC Part B) | Abandoned before Rust (VAROS_START_HERE: "IGNORE PART B") |
| **Mode-based editing** (double-click to enter a shape) | Constitution / VAROS_START_HERE §2: modeless forever |
| **`.varos` zip/OPC container** (DETAILED Save §1, D1) | **ADR-0003**: `.vrs` = PDF + embedded JSON |
| **CBOR model blob + krilla** (SAVE_EXPORT_PLAN §3) | ADR-0003/0004 (versioned JSON); code uses `pdf-writer` (`varos-pdf/src/lib.rs:20-21`) |
| **Floating-shell look** — 12px radii, drop shadows, panels floating over the board (UI_FIGMA_SPEC, UI_MASTER_PLAN, DESIGN_BRIEF, DETAILED A1.5/A2.1) | **UI_DIRECTION** 2026-07-03: homes docked, no shadows, 3px/8px corners |
| Right dock **"Design \| Layers"** tabs (UI_FIGMA_SPEC §2) | PANELS_PRO_SPEC 2026-06-26, then UI_DIRECTION's two tab groups |
| **egui::Window + `egui_tool_windows`** floating panels (GPU_UI_SPIKE_BRIEF) | BOX_SYSTEM_PLAN: `egui_tiles` box tree (ADR-0006) |
| **Single introspectable schema as current law** (old CLAUDE.md, MASTER_PLAN §5.2, DETAILED B6) | **ADR-0004**: future work, not promised |
| **Masks "deferred indefinitely"** (LAYERS_VISION §9.4, 2026-07-06) | Reversed: MASKS_PLAN 2026-07-09, stages 1-2 built `9f0c830`; charter §8 puts masks first |
| **"Pains first — no new front"** rule (MASTER_PLAN header, 2026-07-08) and the audit's **"trust repair first"** (audit §15 Phase 1) | Charter §8 (2026-07-11): **foundation before features and repair** |
| Audit §15 Phase 6 **import-first** product order | Charter §8 proposal: masks 3-6 → PNG → SVG export → SVG import (ratify at F8) |
| **Kashida launch video as L2** (MASTER_PLAN §6.1) | Kashida deferred by Ahmed 2026-07-07 (MASTER_PLAN header); L2 has no date |
| **"Mac/Linux post-v1"** (ROADMAP, VAROS_PLAN, Constitution #5) | STATUS 2026-09-23: Mac is the primary machine; port in flight (Windows stays first) |
| **Hand-test every stage before the next** (CLAUDE.md, Constitution #16) | Amended for the current autonomous run by STATUS 2026-09-23 #2 (green gates + Codex review, batch hand-tests after) |
| **"No push unless told"** (CLAUDE.md process laws) | STATUS 2026-09-23 #3: push after each good commit — **CLAUDE.md not yet synced** (contradiction inside current docs) |
| `plan.html` / DETAILED_ROADMAP as **live status** | Charter §3: STATUS is the only current-state page (audit §7.2 found plan.html stale) |
| **A13 zoom glide** (built 2026-07-08, `2b25f8b`) | Owner 2026-07-12: remove it — P11.2 item 0 (not done yet) |

---

## 6. Gaps — needed, but planned nowhere (or only in a study)

- **G1 · Mac packaging, signing, notarization.** Only in the ONLINE study §3 (items 11-13, unverified prices and APIs). No work order, no ADR, no owner for the Apple Developer account. Needed before anyone else can run the Mac build without a Gatekeeper block.
- **G2 · Windows installer + code signing.** Only in historical MASTER_PLAN B2/B5 and §3.4. No current work order; `flag.release-milestone` is false. SmartScreen will scare every tester (MASTER_PLAN §0 risk #2).
- **G3 · Fonts strategy.** No plan anywhere covers: which fonts ship with the app (the UI reads fonts from `C:/Windows/Fonts`, `ui.rs:1392`, so Mac falls back silently — ONLINE study §3 item 2), font licences, font management for the future Text system, font embedding/subsetting in `.vrs`/PDF, or fonts in a browser build (a web page cannot read installed fonts — ONLINE study §4.4). **Ahmed's idea (2026-09-23), recorded here and parked until the desktop app is stable:** users upload their own fonts to a free cloud drive (e.g. Google Drive), or Drive becomes the primary storage the way WhatsApp backs up to Drive. Any version of this touches ADR-0001 (online) and the Constitution #6 "offline + local files" principle, so it needs its own study and ADR later.
- **G4 · Work-loss protection.** Close-without-save guard, autosave/recovery and `fsync` are parked behind `flag.design-work-started` (charter §8) and are verified absent (§3.7). Real risk the moment Ahmed does real design work in Varos; nothing schedules the flag flip.
- **G5 · The full Stroke spec** (FR-5) and **the Appearance study** (FR-3) were promised as documents "before code" and do not exist.
- **G6 · `VRS_FORMAT.md`** (charter §9.2, MASTER_PLAN §5.2) — the public file-format doc — not written.
- **G7 · `codex/p6-header` lives only on the external disk mirror** (`/Volumes/My Disk/AI workspace/VAROS`, which also holds 18 other `codex/*`/`feat/*` branches). It is not on GitHub. If that disk is lost, the P6 work is lost — and P6's fate is a precondition for F5.
- **G8 · Work-order texts for P11.2 and F4.2 exist only as one-line summaries in STATUS** (commits `e6a1a95`, `8f044c9` touched only GATE_LOG/STATUS). The charter says truth lives in the repo; the full orders should be committed.
- **G9 · Gate tooling on the Mac.** `tools/check_links.ps1`, `check_dep_directions.ps1`, `check_vendor_patches.ps1` need PowerShell; STATUS 2026-09-23 notes `pwsh` is absent. The ADR-0005 CI gate cannot run locally on the primary machine.
- **G10 · Stale current docs.** README "Coming next" still lists the new UI as "being merged"; STATUS dashboard `ui.rs` = 5,563 lines (real: 5,811); CLAUDE.md "no push" vs STATUS push policy; OWNERSHIP_MAP line ranges predate P11.1.
- **G11 · Queue ambiguity after P11.2.** STATUS "Earlier" says F4.2 is queued right after P11.2; STATUS 2026-09-23 #4 says "P11.2, then the remaining PAINS_LOG items". Ahmed should confirm which comes first.
- **G12 · Versioning and About.** No version shown anywhere (0 hits for `CARGO_PKG_VERSION`), no tags, no changelog — beta feedback cannot name a build.
- **G13 · Arabic in the UI itself.** egui has no Arabic shaping (GPU_UI_SPIKE_BRIEF scope guards), so Arabic layer names or an RTL UI option (MASTER_PLAN §5.3) have no plan, even though Varos is "Arabic-first".
- **G14 · Images.** MASTER_PLAN §10 made "Arc-wrap image bytes before images enter" binding, and the Compositor study orders place → clip → crop → trace post-v1, but no plan owns the image node type, asset storage in `.vrs`, or undo cost.
- **G15 · Shape Builder / cut tools timing.** On the Constitution's v1 line ("combine") and in every historical build order, but absent from the charter §8 product sequence.

— End of map. Compiled by an AI session on 2026-09-23 from repository evidence only; no code or existing document was changed.
