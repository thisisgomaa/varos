# ---- Rust build output (huge, regenerated) ----
target/

# ---- Node / web (Tauri front-end, if any) ----
node_modules/
dist/
build/

# ---- Scratch / redirected build logs / temp ----
err.txt
out.txt
*.log
*.tmp
~ai-*

# ---- Adobe Illustrator cursor PNGs (proprietary, TEMP local-dev only — never commit/ship) ----
# Extracted from Adobe's dvaui.dll for Ahmed's local feel-testing. The shippable cursor set is
# the legal SVG one (Font Awesome / Lucide / our own) rendered in cursors.rs.
assets/cursors-ai/
**/cursors-ai/

# ---- OS junk ----
Thumbs.db
Desktop.ini
.DS_Store

# ---- Editor / IDE ----
.vscode/
.idea/
*.swp
*.swo
*~

# ---- Claude Code tooling / session + scratch dumps ----
.claude/
/repo.json
/tree.json
/_cmp.html
